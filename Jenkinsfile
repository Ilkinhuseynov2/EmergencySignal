pipeline {
    agent any
    options {
        skipStagesAfterUnstable()
    }
    stages {
        stage('Build') {
            steps {
                echo "hello world"
                // script {
                //     app = docker.build('fast-api:latest')
                // }
            }
        }
        

    }
}